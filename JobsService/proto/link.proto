syntax="proto3";
option go_package = "/proto";
package proto;

message Request{
    
}

message Response{
    int64 result=1;
}

service CallScrapeService{
    rpc CallScrape(Request) returns(Response);
}